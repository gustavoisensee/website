export type PostType = {
  id: string;
  url: string;
  title: string;
  description: string;
  created_at: string;
};

export type ProjectType = {
  full_name: string;
  description: string;
  html_url: string;
  homepage: string;
  pushed_at: string;
};
